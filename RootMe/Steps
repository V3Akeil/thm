Scan using nmap to find services and use gobuster to get the directories withen the website

use the /panel to upload a reverse shell (php). Use the /uploads to launch the rev shell

find the SUID which is uncommon using the command "find / -perm -u=s -type f 2>/dev/null" which is python

To find the user.txt flag you can use "find -type f -name "user.txt""
